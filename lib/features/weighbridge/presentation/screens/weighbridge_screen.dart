import 'package:flutter/material.dart';

class WeighbridgeScreen extends StatelessWidget {
  const WeighbridgeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Weighbridge Content",
        style: TextStyle(color: Colors.white, fontSize: 18),
      ),
    );
  }
}

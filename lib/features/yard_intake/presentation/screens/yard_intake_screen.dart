import 'package:flutter/material.dart';

class YardIntakeScreen extends StatelessWidget {
  const YardIntakeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Yard Intake Content",
        style: TextStyle(color: Colors.white, fontSize: 18),
      ),
    );
  }
}
